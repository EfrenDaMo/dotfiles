;; Kanata configuration for caps to esc+ctrl

(defsrc
  caps
  ralt
)

(defalias
  escctrl (tap-hold 200 200 esc lctrl)
  altctrl (tap-hold 200 200 ralt rctrl)
)

(deflayer base
  @escctrl
  @altctrl
)
